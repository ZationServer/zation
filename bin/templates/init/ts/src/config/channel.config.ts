import {Config} from 'zation';

exports = Config.channelConfig(
    {
        default :
            {
                publish : false,
                subscribe : true
            },

        customChannels :
            {

            },

        customIdChannels :
            {

            }
    });