import {Config} from 'zation';

export = Config.channelConfig(
    {
        default :
            {
                publish : false,
                subscribe : true
            },

        customChannels :
            {

            },

        customIdChannels :
            {

            }
    });