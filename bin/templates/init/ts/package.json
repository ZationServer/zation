{
  "name": "{{appNameLC}}",
  "version": "{{version}}",
  "description": "{{description}}",
  "license": "{{license}}",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",{{author}}{{git}}
  "scripts": {
    "start": "node dist/index.js",
    "prestart": "npm run build",
    "build": "gulp compile"
  },
  "files": [
    "dist/**/*"
  ],
  "dependencies": {
    "zation": "{{zationVersion}}"
  },
  "devDependencies": {
    "gulp": "3.9.1",
    "gulp-typescript": "5.0.0-alpha.3",
    "typescript": "2.9.2",
    "graceful-fs": "4.1.11",
    "minimatch": "3.0.4"
  }
}
