Name        : Zation
Status      : Alpha
Author      : Luca Scaringella
Description : Application Server Framework

!ZATION!
Version     : 0.5.5
Updates:

IF(V-0.6.0)
ADD create command for example app
ADD config command to check config with json schema
END

IF(FUTURE)
END

!ZATION-SERVER!
Version     : 0.5.5
Updates:

IF(V-0.7.0)
Add option -> zationCrashOnRealException


ADD better isOnline support -> authout -> blocked -> offline
ADD better isOnline support -> refrsh admin panel by addToken/setActivity/ (to many? look every 1sec?)
Save last errors in last 24 hours ?
System Controller for the Panel?

ADD file support (ADD validator Files) (HTTP ONLY)
ADD amazon s3 support
ADD Zation Panel tool ->
-Shows real time request http / socket information
-Connected count, online count
-Server time
-block tokens
-all actions needs to confirm password
-token panel variable
-channel for panel
-worker count
-request per second
-background task information
-last fatal errors
(some information need to be saved on the master)
-worker restart count
-information displayed via chart.js
-panelActionsAllow in main config
-if(allow)
-do emit in all/user/special/group/defaultGroup Channnel from pannel
-show emit to channels in live table
-show userNames there connected

tokenInfoStorage true? && informationStorage true? = panel can true

resetInformationStorage in x hours

ADD zation storage can change to database options

ADD client Js Prepare ->
-on host/zation/client
gets the client js with server host etc.

ADD Online ping for real last activity
END

IF(FUTURE)
?-ADD One of 2 optional params is needed
?-Add controller access for user with id
END

!ZATION-CLIENT!
Version     : 0.5.1
Updates:

IF(V-0.6)
Check All
ADD HTTP Support
[
Client->
Zation.sendHttp(controllerName,params,reaction,token);
zation.getJwtToken();
]
END






